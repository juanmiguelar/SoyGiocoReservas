import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.example.soygioco',
  appName: 'SoyGiocoReservas',
  webDir: 'dist',
  bundledWebRuntime: false
}

export default config
